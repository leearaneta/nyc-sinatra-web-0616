<form method = 'post' action = '/figures'>
  <label>Create a new figure!</label><input type = 'text' name = 'figure[name]' id = 'figure_name'>

  <p>Choose from existing titles:</p>
  <%@titles.each do |title|%>
    <input type = 'checkbox' name = 'figure[title_ids][]' value = '<%=title.id%>' id = 'title_<%=title.id%>'><%=title.name%></input>
  <%end%>
  <p>And/or create a new title:</p>
  <label>Title Name:</label><input type= "text" id = "new_title" name= "title[name]">

  <p>Choose from existing landmarks:</p>
  <%@landmarks.each do |landmark|%>
    <input type = 'checkbox' name = 'figure[landmark_ids][]' value = '<%=landmark.id%>' id = 'landmark_<%=landmark.id%>'><%=landmark.name%></input>
  <%end%>
  <p>And/or create a new landmark:</p>
  <label>Landmark Name:</label><input type = 'text' name = 'landmark[name]' id = 'new_landmark'>
  <label>Year Completed:</label><input type = 'text' name = 'landmark[year_completed]' id = 'new_year_completed'>

  <input type = 'submit' value = 'Create New Figure'>
</form>
